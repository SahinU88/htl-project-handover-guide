# Source Code

This folder contains all the source code of your application.

Please make sure that:

- the complete project is included, so it can be opened and built without missing files
- temporary build output is **not** committed (for example the IDE `bin/` and `obj/` folders)
- any configuration needed to run the project is documented (see [../bin/README.md](./../bin/README.md))

If your project needs special steps to build (for example restoring NuGet packages or a
specific .NET version), describe them shortly here.

---

[Back to the overview](./../README.md)
